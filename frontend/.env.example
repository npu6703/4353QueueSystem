# Frontend env. Copy to `.env.local` (gitignored by default in Vite) and fill in.
#
# Set this to the public URL of your deployed backend. When unset, the app
# falls back to http://localhost:3001 (local dev).
VITE_API_URL=https://your-railway-backend.up.railway.app
